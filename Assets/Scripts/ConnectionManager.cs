using UnityEngine;
public class ConnectionManager : MonoBehaviour
{
    /*public GameObject playerPrefab; // Prefabınızı bu alanda atayın.

    void Start()
    {
        PhotonNetwork.ConnectUsingSettings(); // Photon'a bağlan
    }

    public override void OnConnectedToMaster()
    {
        // Photon sunucusuna bağlandığında çağırılacak
        Debug.Log("Connected to Photon Server");

        // Oyuncuyu oluştur ve ağda senkronize et
        SpawnPlayer();
    }

    private void SpawnPlayer()
    {
        // Oyuncunun pozisyonunu belirleyin
        Vector3 spawnPosition = new Vector3(0f, 0f, 0f);

        // Play objenizi prefab olarak ağda oluşturun
        PhotonNetwork.Instantiate(playerPrefab.name, spawnPosition, Quaternion.identity);
    }*/
}
